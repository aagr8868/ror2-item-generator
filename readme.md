This project is still a work in progress and will not do anything usefull at the moment

First, Clone the repository

Then run this command in the root folder

npm install react@latest react-dom@latest next@latest

you should see the .next and node_midules files in the repository now

to launch the server, run

npm run dev to run on local host

then to open the server on the local site run

open http://localhost:3000/
